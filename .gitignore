# Catalogue shards fetched on demand by model.py / model2c.py
sh-PRIMARY.json
sh-SECONDARY.json
sh-RANGE.json

# Python build artifacts
__pycache__/
*.pyc
residual.txt

# A session-scoped absolute symlink was committed here on 29 Aug and removed on
# 31 Aug. It resolved on one container only. Never commit a path under /tmp.
corpus/corpus
